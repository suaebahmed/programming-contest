#include<bits/stdc++.h>

#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define FOR(i,a,b) for(int i=a; i<= b; ++i)
#define all(v) (v).begin(), (v).end()
#define fi first
#define se second
#define Ld long double

using namespace std;
typedef long long ll;

int solve(vector<int> a,int n,int target)
{

    return 0;
}

int main(){
    optimize();
    int t=1;
    //cin>>t;
    while(t--)
    {
        int n=3;
        //cin>>n;
        vector<int> v={3,2,4};
        int target = 6;
        v.erase(v.begin()+0);
        //FOR(i,0,n-1) cin>>v[i];
        for(auto vl: v) cout<<vl<<" ";
        //cout<<solve(v,n,target)<<endl;
    }
    return 0;
}
